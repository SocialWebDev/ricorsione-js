Benvenuto nel repository "ricorsione-js" di SocialWebDev!

Questo codice spiega il concetto della ricorsione.

Una volta clonato il repo, eseguire da terminale il comando "yarn" per l'installazione dei "node_modules".

COMANDI UTILI:

npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch --> Fa la build di tailwind, tenerlo acceso per sviluppo
